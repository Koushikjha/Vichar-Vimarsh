spring:
  datasource:
    username: root
    password: rootroot

VV:
  jwt:
    secret: dev-secret-32-characters-minimum-xx

app:
  security:
    public-urls:
      - /login
      - /register
      - /saveuser
      - /css/**
      - /js/**
      - /api/v1/auth/send-otp
      - /api/v1/auth/verify-otp
      - /api/v1/auth/refresh
      - /actuator/health
      - /ws/**
      - /favicon.ico